
const initialState = {
    appName:"PostHandler",
    version:"0.0.1",
    decription:"Unuseful app, but great exercise"
}

export const appInfoReducer = function(state = initialState, action){

    return state
}
